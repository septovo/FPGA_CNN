# Zynq-7020 手写数字识别系统：完整实现路径

> 目标：在现有 OV5640 → DDR → RGB LCD 工程上，实现 PL 图像采集与预处理、PS 端 CNN 数字识别、LCD 识别结果叠加，并形成可复现的代码、测试记录和演示。本文是实施与验收清单，不代表这些功能已经完成。

## 1. 已确定的基线与边界

| 项目 | 本工程现状／决定 |
|---|---|
| FPGA/处理器 | `xc7z020clg400-2`，Zynq-7020；Vivado/Vitis 2020.2；PS 为裸机应用。 |
| 输入输出 | OV5640 的 CAM0 输入；RGB LCD 输出。当前软件只初始化 CAM0。 |
| 现有视频链 | CAM0 → `v_vid_in_axi4s_0` → `axi_vdma_0` S2MM → DDR 三帧 RGB888 → `axi_vdma_2` MM2S → `v_axi4s_vid_out_0` → LCD。采集与显示使用同一组三帧缓冲，显示 VDMA 与采集 VDMA 通过 Genlock 同步。 |
| 可利用资源 | BD 内已有未在当前软件启动的 `axi_vdma_1` S2MM、CAM1 输入链。计划复用 `axi_vdma_1` 写入**独立的预处理帧缓冲**；需在 BD 中改接输入，不能只改 C 代码。 |
| 选定模型 | [alankrantas/MNIST-Live-Detection-TFLite](https://github.com/alankrantas/MNIST-Live-Detection-TFLite) 的 `mnist.tflite`。原仓库已归档，实施时固定 Git commit、保存模型 SHA-256、许可证和模型副本。 |
| 首版演示范围 | 一张纸上一个清晰的手写数字 0–9，处于预设 ROI 内；LCD 同时显示实时图像、数字框、类别和置信度。多数字识别作为扩展，在首版通过后实现。 |

现有源文件：`dual_ov5640_lcd.srcs/sources_1/bd/system/system.bd`、`ip_repo/ov5640_capture_data/src/ov5640_capture_data.v`、`vitis/dual_ov5640_lcd/src/main.c`、`vitis/dual_ov5640_lcd/src/vdma_api/vdma_api.c`。每次硬件改动后重新生成 XSA、更新 Vitis platform，并核对 `xparameters.h`，避免软件使用旧地址或旧中断号。

## 2. 最终数据流与模块职责

```text
OV5640 CAM0
  → 现有采集模块 → 原生视频像素复制
                                            ├→ 原 RGB VDMA0 → RGB DDR 三帧 → VDMA2 → AXI-Stream OSD → LCD
                                            └→ RGB 转灰度 → 可选 3×3 均值滤波
                                                → 灰度复制为 RGB888 → VDMA1 → 独立 DDR 三帧
                                                                                   ↓
PS：帧完成中断 → 复制稳定 ROI → 二值化/连通域 → 28×28 uint8 → CNN 推理
   → 结果寄存器（框、数字、置信度）→ OSD 在帧边界更新
```

**为什么灰度暂时仍用 RGB888 存储：** 当前 `axi_vdma_1` 的 S2MM 流宽为 24 位，首版把灰度 Y 复制为 `{Y,Y,Y}`，可复用现有 VDMA 参数与三个字节每像素的驱动。PS 只读其中一个通道。8 位灰度帧能节省 DDR 带宽，但需重新配置 VDMA/流宽/stride，放到性能优化阶段。模型只需要 ROI，PS 不必扫描整张帧。

**为什么不直接在原 RGB 缓冲绘字：** 现有采集和显示 VDMA 共用缓冲。PS 在其中任意位置写像素，可能与采集写入或显示读取冲突。OSD 放在显示 VDMA 后的 AXI4-Stream 上，由 PS 通过 AXI-Lite 提交结果；OSD 在帧开始时锁存新结果。

## 3. 模型接口和移植原则

所选仓库的示例显示：模型输入为 `[1,28,28]`、`uint8`，输出为 `[1,10]`、`float32`。原 Python 管线为灰度 → Otsu 反相二值化 → 5×5 闭运算 → 外轮廓与框筛选 → 补黑边成近似正方形 → `INTER_AREA` 缩放至 28×28 → 推理；示例阈值为 0.7。这些细节以**固定版本的实际 `.tflite` 文件和源代码复核结果**为准，不凭文件扩展名推断量化类型。原仓库的公开测试数字约 98.97% 是 MNIST 测试集成绩，不是本摄像头画面的保证值。

当前 PS 是裸机 C 工程，不能把 Python/OpenCV 脚本或 `.tflite` 文件直接加入 `main.c` 就运行。优先路线：检查模型 FlatBuffer 的算子、张量形状、数据类型和内存需求；在 Vitis 中移植能支持全部算子的 TensorFlow Lite Micro 或等价精简解释器。若该模型含裸机解释器不支持的算子，则从**同一模型文件**导出权重与算子参数，以 C/C++ 实现同等计算图；每层输出与 PC 基准对齐。更换模型、重训或修改网络结构均需另行记录，不作为“已部署所选模型”冒充验收。

## 4. 建议代码目录与接口

在现有工程中新增下列源文件；路径是建议，落地时保持 `main.c` 中只有初始化、状态机和主循环：

```text
ip_repo/gray_mean_video/src/gray_mean_video.v      # CAM0 原生视频 RGB→Y、3×3 均值、旁路
ip_repo/digit_osd_axis/src/digit_osd_axis.v        # AXI4-Stream 框/数字/置信度叠加，AXI-Lite 寄存器
vitis/dual_ov5640_lcd/src/frame_pipeline.c/.h    # VDMA1、帧中断、缓冲状态、错误计数
vitis/dual_ov5640_lcd/src/digit_preprocess.c/.h  # ROI、阈值、连通域、补边和缩放
vitis/dual_ov5640_lcd/src/digit_model.c/.h       # 所选模型装载／推理，返回 10 类分数
vitis/dual_ov5640_lcd/src/digit_osd.c/.h         # 结果寄存器写入与帧边界提交
vitis/dual_ov5640_lcd/src/diagnostics.c/.h       # 串口日志、耗时、帧率和错误统计
tests/                                       # PC 黄金数据、HDL testbench、实拍测试样本及脚本
docs/                                        # 接线、寄存器表、内存表、测试记录和报告素材
```

所有模块的头文件写明：输入格式、stride、宽高、坐标系、缓冲区所有权和返回错误码。视频链保留可切换的“原样直通”模式，方便定位 PL 改动导致的故障。

## 5. 分阶段实施与测试

### 阶段 0：基线冻结与硬件清点

**编码与配置**

1. 备份当前 XPR/XSA/ELF/bitstream 和关键源文件；记录 Vivado/Vitis 版本、板卡 DDR 容量、LCD ID、当前分辨率、CAM0 设置。
2. 用现有应用重新完成 CAM0 直通 LCD 启动。检查 `system.bd` 中 VDMA0、VDMA1、VDMA2 的真实连接、时钟、复位、Genlock、HP0 和中断端口；记录综合资源余量。
3. 建立 `docs/hardware_baseline.md`：IP 地址、中断号、视频像素格式、三帧地址与 stride。建立 `docs/ddr_map.md`，为新缓冲、ROI 暂存、模型和栈划定互不重叠区域。当前代码使用 `DDR_BASE + 0x01000000` 作为原 RGB 环起点；新区域须核对 ELF map/linker、实际 DDR 物理范围、最大 1280×800 模式和对齐要求后再定。

**测试与通过标准**

- 冷启动 10 次：摄像头检测、LCD ID、分辨率均正确，画面无黑屏、花屏。
- 连续显示 ≥30 分钟；串口记录 VDMA 状态，未出现错误、复位或明显撕裂。
- 保存基线照片、串口日志、BD 截图和综合资源表。失败时先修复基线，不进入后续阶段。

### 阶段 1：固定模型并建立 PC 黄金基准

**编码与配置**

1. 固定上游仓库 commit、复制 `mnist.tflite`、记录 SHA-256 与许可证。使用其 `mnist_tflite_model_test.py` 在 PC 上核对官方样例；另写 `tests/model_inspect.py` 输出所有输入/输出张量及算子列表。
2. 写 `tests/golden_inference.py`：对至少 100 张 MNIST 测试图和 0–9 各类实拍样本，保存最终 28×28 输入字节、10 类输出向量和预测标签。实拍样本按拍摄场景编号，训练/验证/现场验收三组不混用。
3. 先在 PC 用原脚本跑通纸面数字；保存每一步中间图。把 Otsu、形态学、裁剪、补边、缩放参数写成明确规格，避免移植时“看起来相似”但输入不同。

**测试与通过标准**

- 模型检查得到确定的输入形状、dtype、算子表和内存需求；模型 SHA-256 可复核。
- 原始模型在 MNIST 测试集重现仓库报告的量级；若环境版本造成差异，记录版本和实际结果，不能改写成绩。
- PC 上每个 0–9 类至少有可保存、可重复推理的实拍样本；确认黑底白字方向与模型一致。

### 阶段 2：PL 灰度和均值滤波链

**编码与配置**

1. 在 CAM0 采集模块的原生 24 位视频输出上分支。新增灰度模块：整数近似 `Y=(77R+150G+29B+128)>>8`；滤波开关由 AXI GPIO 第 2 通道控制。3×3 均值用两行缓冲和 9 像素累加，前两行/列直通灰度。
2. 两条分支各自经过现有 Video In to AXI4-Stream IP。灰度模块在原生视频时钟域使用 `vid_ce`、`vid_active_video`、`vid_vsync`；下游 AXI 背压由 Video In IP 的 FIFO 处理。验证行/帧同步和输出像素计数。
3. 将处理后的 Y 复制为 RGB888，接至 VDMA1 S2MM；保持原 RGB 显示链独立。重新 Validate Design、生成 bitstream 和 XSA。

**测试与通过标准**

- HDL 仿真覆盖：黑/白/原色、棋盘、边缘像素、连续帧、复位发生在行中、随机 `vid_ce` 停顿及下游 Video In IP 的 AXI 背压。与 PC 整数参考实现逐像素比较；预期一致率 100%。
- 检查每帧像素数、每行 `TLAST` 位置、帧首 `TUSER` 次数；不得丢行、重行、跨帧串数据。
- 综合/实现通过，时序无负裕量；记录 LUT/FF/BRAM 增量。上板时在串口或导出帧中比较滤波开/关效果。

### 阶段 3：预处理帧进入 PS，建立安全帧调度

**编码与配置**

1. `frame_pipeline.c` 初始化 VDMA1 的独立三帧地址、实际 HSIZE/VSize/stride，并接通帧完成与错误中断到 GIC。核对 VDMA1 当前 Genlock 从模式与 VDMA0 帧指针连接，不能只按循环计数猜测“刚完成的缓冲编号”。
2. 中断服务程序只确认中断、记录帧序号/VDMA 状态/缓冲索引并发布事件；耗时处理在主循环执行。主循环只从已完成且当前未被 DMA 写的帧复制 ROI 到独立暂存区，然后释放。跟不上摄像头时丢旧帧，始终处理最新稳定帧。
3. DMA 写后、CPU 读取前对对应范围做 D-cache invalidate；禁止把 PS 脏缓存行 flush 回 DMA 刚写的区域。整理 DDR 分配和 linker，防止缓冲覆盖程序、堆、栈。增加超时、VDMA 错误计数和恢复路径。

**测试与通过标准**

- 对 VDMA1 的三个缓冲分别验证地址、帧序号、首末行、stride；拍摄移动物体时连续采样无“上半帧新、下半帧旧”。
- 使用固定图案和实拍画面验证灰度像素与 PL 黄金数据一致；覆盖全部支持的 LCD 分辨率，至少首选演示分辨率 800×480 必须通过。
- 连续运行 ≥30 分钟，无 VDMA error、DDR 越界、缓存导致的旧图；记录丢帧数和 ROI 复制时间。

### 阶段 4：PS 图像预处理与候选数字提取

**编码与配置**

1. 在 `digit_preprocess.c` 实现 ROI 裁剪、直方图/Otsu 阈值、反相、5×5 闭运算、连通域或外轮廓、面积与边界过滤。首版每帧选择 ROI 内最可信的单个连通域；空白画面输出“无数字”。
2. 按原脚本的补边规则形成正方形，再做等效面积缩小至 28×28；将目标字节逐项传给模型。若因实时性改用其他缩放法，先用黄金样本比较输出变化并记录，不直接替换。
3. 所有坐标为传感器/LCD 原始像素坐标。限制候选数量和极端输入尺寸，防止噪声触发超时；保存诊断开关，可把 28×28 字节经串口/文件导出。

**测试与通过标准**

- 至少 30 张固定图片含空白、0–9、靠近边界、细笔画、粗笔画、斜写和阴影；核对候选框、二值图和最终输入。固定图片的结果必须可重复。
- 与阶段 1 的 PC 黄金输入逐字节比较；算法允许差异必须有记录和对最终类别的影响分析。
- 空白、无效 ROI、过多噪声不能越界或死循环；记录每阶段耗时。

### 阶段 5：PS 上运行选定 CNN 模型

**编码与配置**

1. 先在 PC 做移植可行性检查：列出 FlatBuffer 算子、版本、张量数、峰值工作内存和实际输入输出类型。以这一列表决定裸机解释器及其需要注册的算子，或同模型权重的 C/C++ 计算图实现。
2. `digit_model.c` 提供 `init`、`infer(const uint8_t[784], float[10])`、错误返回与模型版本查询。若模型输入含 `uint8` 非量化张量，保持原值；不要自行除以 255 或用错误的 zero-point。以阶段 1 的张量检查结果确定最终代码。
3. 启动时报告模型 SHA-256/版本、内存占用和算子初始化状态；推理时检查 NaN、10 类输出范围及超时。CPU 负载超限时先降低**识别频率**，维持显示连续。

**测试与通过标准**

- PC 黄金集逐例比对 ARM 与 PC 的 10 类输出：同精度实现设定数值容差；不同精度实现至少保持预测类别一致，分歧样本逐例分析。容差在实现前写入测试脚本。
- 单独在 ARM 上运行 0–9、空白和重复 1000 次推理；不得内存泄漏、输出漂移或死机。
- 记录平均、P95、最大推理时间和峰值内存；实测是否满足所选识别频率。若不能部署，阶段 5 不能标记通过，应记录不支持算子/内存/构建错误并修复。

### 阶段 6：LCD 框与数字叠加

**编码与配置**

1. 将 `digit_osd_axis` 插入 `axi_vdma_2/M_AXIS_MM2S` 与 `v_axi4s_vid_out_0/video_in` 之间。实现 0–9 字模、框线、置信度（可先以整数百分比显示）和“无数字”状态；背景 RGB 像素保持原值。
2. AXI-Lite 寄存器定义 `x,y,w,h,class,score,valid,commit`；PS 写影子寄存器，OSD 只在 `TUSER` 帧首锁存，避免半帧显示旧结果、半帧显示新结果。多时钟域采用可靠的同步/握手机制。
3. 结果带帧序号和超时值。识别结果超时则清除框或标注“等待识别”，避免画面变了却显示陈旧数字。

**测试与通过标准**

- 仿真检查 0/9、边缘框、画面外坐标、同一帧中更新寄存器、随机背压；输出像素数与 `TUSER/TLAST` 完全保持。
- 上板展示摄像头实时图像及正确位置的框/数字；快速切换纸面数字时无半帧叠加、坐标越界或画面中断。
- OSD 禁用时与阶段 0 图像链一致；持续显示 ≥30 分钟无 VDMA/Video Out 错误。

### 阶段 7：联合调试、性能和验收

**编码与配置**

1. `main.c` 按顺序初始化：时钟/摄像头 → RGB 显示链 → 灰度链 → 帧中断 → 模型 → OSD；各步失败都有串口错误码与安全退出/重试。增加 UART 命令切换直通、灰度、滤波、二值、识别、OSD 诊断模式。
2. 建立 `tests/field_test.csv`：每张测试样本写明真值、预测、置信度、光照、笔迹、距离和是否定位成功。区分“没有找到数字”和“找到了但分类错误”。
3. 在串口统计采集帧率、处理帧率、每阶段耗时、端到端延迟、丢帧、VDMA 错误及内存峰值。时延用 LED/屏幕同步拍摄或测试图切换法实测，不用单次推理耗时冒充端到端延迟。

**测试与通过标准**

- 10 类各至少 30 张**此前未用于调参的实拍测试图**；输出混淆矩阵、总体准确率、各类召回率、定位成功率。建议项目目标：受控光照、单数字、固定 ROI 下识别准确率 ≥90%；这是设计目标，须以实际测试结果报告，不能预填成实绩。
- 验证普通室内光、偏暗、轻微倾斜、不同笔宽、背景阴影与快速换纸；列出失败样例及改进。识别频率目标可先定 ≥5 次/秒，显示保持原链路稳定；最终以实测为准。
- 冷启动 10 次、连续运行 ≥2 小时；无死机、异常重启、持续增长的错误数或显示撕裂。录制从上电到 0–9 识别与叠加的完整演示。

## 6. 编码规范与交付清单

**编码规范：** Verilog 模块使用明确位宽、同步复位释放和完整时序注释；AXI4-Stream 信号在背压时保持稳定。C/C++ 模块使用固定宽度类型、边界检查、显式错误码、独立头文件；ISR 不做图像处理或推理。模型输入输出、DDR 布局、寄存器、时钟域、缓存操作和第三方来源写成文档。上游第三方代码保留许可证和归属说明。

**最终交付：** 可打开的 Vivado 工程与自定义 IP、bitstream/XSA、Vitis 源码与 ELF、固定的模型文件及 SHA-256、PC 黄金测试脚本/样本、PL 仿真、阶段测试记录、技术报告、功能演示视频。报告需包含系统框图、模块接口、DDR 布局、模型来源与移植方式、时序/资源、准确率和性能实测、失败案例及复现步骤。

## 7. 风险及处理顺序

1. **模型裸机算子兼容性：** 阶段 1 即检查 FlatBuffer，阶段 5 必须完成 PC/ARM 输出对齐。若解释器不支持，先实现同模型权重的运算，不在验收前临时替换模型。
2. **真实摄像头与 MNIST 的差异：** 优先保证白纸黑字、受控光照和一致的二值化/补边；记录实拍测试。准确率不足时用实拍样本调节预处理；如最终需要微调模型，保留原始模型与新版本成绩，明示版本变化。
3. **缓冲区覆盖与缓存：** 阶段 0 固定内存表，阶段 3 先证明“完整帧 + 正确索引 + 缓存一致”，再接识别；不要在 DMA 正在写的帧上推理。
4. **DDR 与 CPU 带宽：** 首版 24 位灰度缓冲占较多带宽；如测量发现瓶颈，再改成 8 位灰度或减小采集 ROI/分辨率，并重新测试整条链路。
5. **OSD 与帧同步：** 结果寄存器在帧首统一生效，识别完成频率与显示帧率解耦；长时间未得到新结果时清除旧结果。

## 参考资料

- 所选模型及演示代码：[MNIST-Live-Detection-TFLite](https://github.com/alankrantas/MNIST-Live-Detection-TFLite)、[静态图片预处理与推理脚本](https://github.com/alankrantas/MNIST-Live-Detection-TFLite/blob/main/mnist_tflite_detection.py)。
- AMD [AXI VDMA Genlock 同步说明](https://docs.amd.com/r/en-US/pg020_axi_vdma/Genlock-Synchronization)：帧缓冲的生产者与消费者不应同时访问同一帧。

